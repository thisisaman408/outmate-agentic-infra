# outmate Service Configuration
#
# This file tells LFX to use outmate's full-featured service implementations
# instead of the minimal/noop services built into LFX.
#
# Usage:
#   1. Copy this file to your outmate_CONFIG_DIR as lfx.toml
#   2. Or add to pyproject.toml under [tool.lfx.services]
#
# The services will be automatically discovered and loaded when you run:
#   - outmate run
#   - lfx serve (when run from outmate project)

[services]
# Core Services - Full-featured outmate implementations
database_service = "outmate.services.database.service:DatabaseService"
storage_service = "outmate.services.storage.local:LocalStorageService"
cache_service = "outmate.services.cache.service:ThreadingInMemoryCache"

# Authentication & Session Management
auth_service = "outmate.services.auth.service:AuthService"
session_service = "outmate.services.session.service:SessionService"

# Chat & Communication
chat_service = "outmate.services.chat.service:ChatService"

# State & Data Management
state_service = "outmate.services.state.service:StateService"
variable_service = "outmate.services.variable.service:VariableService"

# Background Processing
task_service = "outmate.services.task.service:TaskService"
job_queue_service = "outmate.services.job_queue.service:JobQueueService"

# Marketplace & Components
store_service = "outmate.services.store.service:StoreService"
shared_component_cache_service = "outmate.services.shared_component_cache.service:SharedComponentCacheService"

# Observability
telemetry_service = "outmate.services.telemetry.service:TelemetryService"
tracing_service = "outmate.services.tracing.service:TracingService"

# Note: settings_service is NOT configurable - it always uses the built-in implementation
# Note: mcp_composer_service uses the lfx implementation by default
